# NEVER commit these — they would leak the password or plaintext holdings
dash_secret.txt
last_payload.json
holdings.local.json*

# build output (regenerated by the workflow each run; deployed to Vercel)
public/

# misc
.vercel/
__pycache__/
*.pyc
.DS_Store
